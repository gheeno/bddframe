# Copy to secrets.env (gitignored — never commit it) and fill in.
# Referenced as [BB_USER] etc. in this folder's .feature files.
# Package-scoped — see ../../../../docs/feature-packages.md for the resolution order.

BB_USER=reel_ryan
BB_PASS=Popcorn1!
