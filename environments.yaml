# Base URLs per app/environment. Safe to commit — no secrets here.
# Top-level keys become [KEY] references in .feature files (e.g. [SAUCEDEMO]).
# Real env vars / .env win over these, so CI can override without editing this file.

saucedemo: https://www.saucedemo.com
staging: https://staging.example.com
