# Committed config — NO SECRETS here.
# Credentials/tokens go in secrets.env (gitignored). See secrets.env.example.
# Base URLs live in environments.yaml.

NOODLE_HEADLESS=false
NOODLE_BROWSER=chromium
NOODLE_TIMEOUT=10000
