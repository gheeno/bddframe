# Local POM — applies ONLY to features/saucedemo/*.feature files.
# Overrides global pom.yaml for any matching key.
#
# Saucedemo-specific elements with no accessible text label:

burger menu:
  id: react-burger-menu-btn

shopping cart:
  css: ".shopping_cart_link"

product list:
  css: ".inventory_list"

cart badge:
  css: ".shopping_cart_badge"
