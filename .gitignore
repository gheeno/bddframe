# .env is committed (config only, no secrets). Secrets live in secrets.env.
secrets.env
# Per-app secrets — features/<app>/environment/secrets.env (see docs/feature-packages.md)
features/**/environment/secrets.env
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
screenshots/
allure-results/
allure-report/
traces/
videos/
output/
healing-report.txt
healing-report.jsonl
healing.jsonl
# Squad: ignore runtime state (logs, inbox, sessions)
.squad/orchestration-log/
.squad/log/
.squad/decisions/inbox/
.squad/sessions/
.squad/.scratch/
# Squad: SubSquad activation file (local to this machine)
.squad-workstream
# Squad: ignore runtime state (logs, inbox, sessions)
.squad/.cache/
